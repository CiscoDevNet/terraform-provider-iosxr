---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "iosxr_macsec Resource - terraform-provider-iosxr"
subcategory: "Security"
description: |-
  This resource can manage the MACSec configuration.
---

# iosxr_macsec (Resource)

This resource can manage the MACSec configuration.

## Example Usage

```terraform
resource "iosxr_macsec" "example" {
  shutdown = true
  fips     = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `delete_mode` (String) Configure behavior when deleting/destroying the resource. Either delete the entire object (YANG container) being managed, or only delete the individual resource attributes configured explicitly and leave everything else as-is. Default value is `all`.
  - Choices: `all`, `attributes`
- `device` (String) A device name from the provider configuration.
- `fips` (Boolean) Enable fips-mode for MACsec
- `shutdown` (Boolean) Disable macsec on all data ports, has no impact on macsec configs

### Read-Only

- `id` (String) The path of the object.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import iosxr_macsec.example ""
```
