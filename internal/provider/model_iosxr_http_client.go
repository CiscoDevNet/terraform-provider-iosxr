// Copyright © 2023 Cisco Systems, Inc. and its affiliates.
// All rights reserved.
//
// Licensed under the Mozilla Public License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//	https://mozilla.org/MPL/2.0/
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// SPDX-License-Identifier: MPL-2.0

// Code generated by "gen/generator.go"; DO NOT EDIT.

package provider

// Section below is generated&owned by "gen/generator.go". //template:begin imports
import (
	"context"
	"fmt"
	"strconv"
	"strings"

	"github.com/CiscoDevNet/terraform-provider-iosxr/internal/provider/helpers"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"
	"github.com/netascode/go-netconf"
	"github.com/netascode/xmldot"
	"github.com/tidwall/gjson"
	"github.com/tidwall/sjson"
)

// End of section. //template:end imports

// Section below is generated&owned by "gen/generator.go". //template:begin types
type HTTPClient struct {
	Device                  types.String `tfsdk:"device"`
	Id                      types.String `tfsdk:"id"`
	DeleteMode              types.String `tfsdk:"delete_mode"`
	Vrf                     types.String `tfsdk:"vrf"`
	SecureVerifyPeerDisable types.Bool   `tfsdk:"secure_verify_peer_disable"`
	SecureVerifyHostDisable types.Bool   `tfsdk:"secure_verify_host_disable"`
	ResponseTimeout         types.Int64  `tfsdk:"response_timeout"`
	ConnectionTimeout       types.Int64  `tfsdk:"connection_timeout"`
	ConnectionRetry         types.Int64  `tfsdk:"connection_retry"`
	SourceInterfaceIpv4     types.String `tfsdk:"source_interface_ipv4"`
	SourceInterfaceIpv6     types.String `tfsdk:"source_interface_ipv6"`
	VersionDefault          types.Bool   `tfsdk:"version_default"`
	Version10               types.Bool   `tfsdk:"version_10"`
	Version11               types.Bool   `tfsdk:"version_11"`
	TcpWindowScale          types.Int64  `tfsdk:"tcp_window_scale"`
	SslVersionTls10         types.Bool   `tfsdk:"ssl_version_tls10"`
	SslVersionTls11         types.Bool   `tfsdk:"ssl_version_tls11"`
	SslVersionTls12         types.Bool   `tfsdk:"ssl_version_tls12"`
	SslVersionTls13         types.Bool   `tfsdk:"ssl_version_tls13"`
}

type HTTPClientData struct {
	Device                  types.String `tfsdk:"device"`
	Id                      types.String `tfsdk:"id"`
	Vrf                     types.String `tfsdk:"vrf"`
	SecureVerifyPeerDisable types.Bool   `tfsdk:"secure_verify_peer_disable"`
	SecureVerifyHostDisable types.Bool   `tfsdk:"secure_verify_host_disable"`
	ResponseTimeout         types.Int64  `tfsdk:"response_timeout"`
	ConnectionTimeout       types.Int64  `tfsdk:"connection_timeout"`
	ConnectionRetry         types.Int64  `tfsdk:"connection_retry"`
	SourceInterfaceIpv4     types.String `tfsdk:"source_interface_ipv4"`
	SourceInterfaceIpv6     types.String `tfsdk:"source_interface_ipv6"`
	VersionDefault          types.Bool   `tfsdk:"version_default"`
	Version10               types.Bool   `tfsdk:"version_10"`
	Version11               types.Bool   `tfsdk:"version_11"`
	TcpWindowScale          types.Int64  `tfsdk:"tcp_window_scale"`
	SslVersionTls10         types.Bool   `tfsdk:"ssl_version_tls10"`
	SslVersionTls11         types.Bool   `tfsdk:"ssl_version_tls11"`
	SslVersionTls12         types.Bool   `tfsdk:"ssl_version_tls12"`
	SslVersionTls13         types.Bool   `tfsdk:"ssl_version_tls13"`
}

// End of section. //template:end types

// Section below is generated&owned by "gen/generator.go". //template:begin getPath

func (data HTTPClient) getPath() string {
	return "Cisco-IOS-XR-um-http-client-cfg:/http/client"
}

func (data HTTPClientData) getPath() string {
	return "Cisco-IOS-XR-um-http-client-cfg:/http/client"
}

// getXPath returns the XPath for NETCONF operations
func (data HTTPClient) getXPath() string {
	path := "Cisco-IOS-XR-um-http-client-cfg:/http/client"
	return path
}

func (data HTTPClientData) getXPath() string {
	path := "Cisco-IOS-XR-um-http-client-cfg:/http/client"
	return path
}

// End of section. //template:end getPath

// Section below is generated&owned by "gen/generator.go". //template:begin toBody

func (data HTTPClient) toBody(ctx context.Context) string {
	body := "{}"
	if !data.Vrf.IsNull() && !data.Vrf.IsUnknown() {
		body, _ = sjson.Set(body, "vrf", data.Vrf.ValueString())
	}
	if !data.SecureVerifyPeerDisable.IsNull() && !data.SecureVerifyPeerDisable.IsUnknown() {
		if data.SecureVerifyPeerDisable.ValueBool() {
			body, _ = sjson.Set(body, "secure-verify-peer.disable", map[string]string{})
		}
	}
	if !data.SecureVerifyHostDisable.IsNull() && !data.SecureVerifyHostDisable.IsUnknown() {
		if data.SecureVerifyHostDisable.ValueBool() {
			body, _ = sjson.Set(body, "secure-verify-host.disable", map[string]string{})
		}
	}
	if !data.ResponseTimeout.IsNull() && !data.ResponseTimeout.IsUnknown() {
		body, _ = sjson.Set(body, "response.timeout", strconv.FormatInt(data.ResponseTimeout.ValueInt64(), 10))
	}
	if !data.ConnectionTimeout.IsNull() && !data.ConnectionTimeout.IsUnknown() {
		body, _ = sjson.Set(body, "connection.timeout", strconv.FormatInt(data.ConnectionTimeout.ValueInt64(), 10))
	}
	if !data.ConnectionRetry.IsNull() && !data.ConnectionRetry.IsUnknown() {
		body, _ = sjson.Set(body, "connection.retry", strconv.FormatInt(data.ConnectionRetry.ValueInt64(), 10))
	}
	if !data.SourceInterfaceIpv4.IsNull() && !data.SourceInterfaceIpv4.IsUnknown() {
		body, _ = sjson.Set(body, "source-interface.ipv4.interface", data.SourceInterfaceIpv4.ValueString())
	}
	if !data.SourceInterfaceIpv6.IsNull() && !data.SourceInterfaceIpv6.IsUnknown() {
		body, _ = sjson.Set(body, "source-interface.ipv6.interface", data.SourceInterfaceIpv6.ValueString())
	}
	if !data.VersionDefault.IsNull() && !data.VersionDefault.IsUnknown() {
		if data.VersionDefault.ValueBool() {
			body, _ = sjson.Set(body, "version.default", map[string]string{})
		}
	}
	if !data.Version10.IsNull() && !data.Version10.IsUnknown() {
		if data.Version10.ValueBool() {
			body, _ = sjson.Set(body, "version.http1\\.0", map[string]string{})
		}
	}
	if !data.Version11.IsNull() && !data.Version11.IsUnknown() {
		if data.Version11.ValueBool() {
			body, _ = sjson.Set(body, "version.http1\\.1", map[string]string{})
		}
	}
	if !data.TcpWindowScale.IsNull() && !data.TcpWindowScale.IsUnknown() {
		body, _ = sjson.Set(body, "tcp-window-scale", strconv.FormatInt(data.TcpWindowScale.ValueInt64(), 10))
	}
	if !data.SslVersionTls10.IsNull() && !data.SslVersionTls10.IsUnknown() {
		if data.SslVersionTls10.ValueBool() {
			body, _ = sjson.Set(body, "ssl.version.tls1\\.0", map[string]string{})
		}
	}
	if !data.SslVersionTls11.IsNull() && !data.SslVersionTls11.IsUnknown() {
		if data.SslVersionTls11.ValueBool() {
			body, _ = sjson.Set(body, "ssl.version.tls1\\.1", map[string]string{})
		}
	}
	if !data.SslVersionTls12.IsNull() && !data.SslVersionTls12.IsUnknown() {
		if data.SslVersionTls12.ValueBool() {
			body, _ = sjson.Set(body, "ssl.version.tls1\\.2", map[string]string{})
		}
	}
	if !data.SslVersionTls13.IsNull() && !data.SslVersionTls13.IsUnknown() {
		if data.SslVersionTls13.ValueBool() {
			body, _ = sjson.Set(body, "ssl.version.tls1\\.3", map[string]string{})
		}
	}
	return body
}

// End of section. //template:end toBody

// Section below is generated&owned by "gen/generator.go". //template:begin updateFromBody

func (data *HTTPClient) updateFromBody(ctx context.Context, res gjson.Result) {
	if value := res.Get("vrf"); value.Exists() && !data.Vrf.IsNull() {
		data.Vrf = types.StringValue(value.String())
	} else if data.Vrf.IsNull() {
		data.Vrf = types.StringNull()
	}
	if value := res.Get("secure-verify-peer.disable"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SecureVerifyPeerDisable.IsNull() {
			data.SecureVerifyPeerDisable = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SecureVerifyPeerDisable.IsNull() {
			data.SecureVerifyPeerDisable = types.BoolNull()
		}
	}
	if value := res.Get("secure-verify-host.disable"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SecureVerifyHostDisable.IsNull() {
			data.SecureVerifyHostDisable = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SecureVerifyHostDisable.IsNull() {
			data.SecureVerifyHostDisable = types.BoolNull()
		}
	}
	if value := res.Get("response.timeout"); value.Exists() && !data.ResponseTimeout.IsNull() {
		data.ResponseTimeout = types.Int64Value(value.Int())
	} else if data.ResponseTimeout.IsNull() {
		data.ResponseTimeout = types.Int64Null()
	}
	if value := res.Get("connection.timeout"); value.Exists() && !data.ConnectionTimeout.IsNull() {
		data.ConnectionTimeout = types.Int64Value(value.Int())
	} else if data.ConnectionTimeout.IsNull() {
		data.ConnectionTimeout = types.Int64Null()
	}
	if value := res.Get("connection.retry"); value.Exists() && !data.ConnectionRetry.IsNull() {
		data.ConnectionRetry = types.Int64Value(value.Int())
	} else if data.ConnectionRetry.IsNull() {
		data.ConnectionRetry = types.Int64Null()
	}
	if value := res.Get("source-interface.ipv4.interface"); value.Exists() && !data.SourceInterfaceIpv4.IsNull() {
		data.SourceInterfaceIpv4 = types.StringValue(value.String())
	} else if data.SourceInterfaceIpv4.IsNull() {
		data.SourceInterfaceIpv4 = types.StringNull()
	}
	if value := res.Get("source-interface.ipv6.interface"); value.Exists() && !data.SourceInterfaceIpv6.IsNull() {
		data.SourceInterfaceIpv6 = types.StringValue(value.String())
	} else if data.SourceInterfaceIpv6.IsNull() {
		data.SourceInterfaceIpv6 = types.StringNull()
	}
	if value := res.Get("version.default"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.VersionDefault.IsNull() {
			data.VersionDefault = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.VersionDefault.IsNull() {
			data.VersionDefault = types.BoolNull()
		}
	}
	if value := res.Get("version.http1\\.0"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.Version10.IsNull() {
			data.Version10 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.Version10.IsNull() {
			data.Version10 = types.BoolNull()
		}
	}
	if value := res.Get("version.http1\\.1"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.Version11.IsNull() {
			data.Version11 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.Version11.IsNull() {
			data.Version11 = types.BoolNull()
		}
	}
	if value := res.Get("tcp-window-scale"); value.Exists() && !data.TcpWindowScale.IsNull() {
		data.TcpWindowScale = types.Int64Value(value.Int())
	} else if data.TcpWindowScale.IsNull() {
		data.TcpWindowScale = types.Int64Null()
	}
	if value := res.Get("ssl.version.tls1\\.0"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls10.IsNull() {
			data.SslVersionTls10 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls10.IsNull() {
			data.SslVersionTls10 = types.BoolNull()
		}
	}
	if value := res.Get("ssl.version.tls1\\.1"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls11.IsNull() {
			data.SslVersionTls11 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls11.IsNull() {
			data.SslVersionTls11 = types.BoolNull()
		}
	}
	if value := res.Get("ssl.version.tls1\\.2"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls12.IsNull() {
			data.SslVersionTls12 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls12.IsNull() {
			data.SslVersionTls12 = types.BoolNull()
		}
	}
	if value := res.Get("ssl.version.tls1\\.3"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls13.IsNull() {
			data.SslVersionTls13 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls13.IsNull() {
			data.SslVersionTls13 = types.BoolNull()
		}
	}
}

// End of section. //template:end updateFromBody

// Section below is generated&owned by "gen/generator.go". //template:begin fromBody

func (data *HTTPClient) fromBody(ctx context.Context, res gjson.Result) {
	prefix := helpers.LastElement(data.getPath()) + "."
	if res.Get(helpers.LastElement(data.getPath())).IsArray() {
		prefix += "0."
	}
	// Check if data is at root level (gNMI response case)
	if !res.Get(helpers.LastElement(data.getPath())).Exists() {
		prefix = ""
	}
	if value := res.Get(prefix + "vrf"); value.Exists() {
		data.Vrf = types.StringValue(value.String())
	}
	if value := res.Get(prefix + "secure-verify-peer.disable"); value.Exists() {
		data.SecureVerifyPeerDisable = types.BoolValue(true)
	} else if !data.SecureVerifyPeerDisable.IsNull() {
		// Only set to false if it was previously set in state
		data.SecureVerifyPeerDisable = types.BoolValue(false)
	}
	if value := res.Get(prefix + "secure-verify-host.disable"); value.Exists() {
		data.SecureVerifyHostDisable = types.BoolValue(true)
	} else if !data.SecureVerifyHostDisable.IsNull() {
		// Only set to false if it was previously set in state
		data.SecureVerifyHostDisable = types.BoolValue(false)
	}
	if value := res.Get(prefix + "response.timeout"); value.Exists() {
		data.ResponseTimeout = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "connection.timeout"); value.Exists() {
		data.ConnectionTimeout = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "connection.retry"); value.Exists() {
		data.ConnectionRetry = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "source-interface.ipv4.interface"); value.Exists() {
		data.SourceInterfaceIpv4 = types.StringValue(value.String())
	}
	if value := res.Get(prefix + "source-interface.ipv6.interface"); value.Exists() {
		data.SourceInterfaceIpv6 = types.StringValue(value.String())
	}
	if value := res.Get(prefix + "version.default"); value.Exists() {
		data.VersionDefault = types.BoolValue(true)
	} else if !data.VersionDefault.IsNull() {
		// Only set to false if it was previously set in state
		data.VersionDefault = types.BoolValue(false)
	}
	if value := res.Get(prefix + "version.http1\\.0"); value.Exists() {
		data.Version10 = types.BoolValue(true)
	} else if !data.Version10.IsNull() {
		// Only set to false if it was previously set in state
		data.Version10 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "version.http1\\.1"); value.Exists() {
		data.Version11 = types.BoolValue(true)
	} else if !data.Version11.IsNull() {
		// Only set to false if it was previously set in state
		data.Version11 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "tcp-window-scale"); value.Exists() {
		data.TcpWindowScale = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.0"); value.Exists() {
		data.SslVersionTls10 = types.BoolValue(true)
	} else if !data.SslVersionTls10.IsNull() {
		// Only set to false if it was previously set in state
		data.SslVersionTls10 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.1"); value.Exists() {
		data.SslVersionTls11 = types.BoolValue(true)
	} else if !data.SslVersionTls11.IsNull() {
		// Only set to false if it was previously set in state
		data.SslVersionTls11 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.2"); value.Exists() {
		data.SslVersionTls12 = types.BoolValue(true)
	} else if !data.SslVersionTls12.IsNull() {
		// Only set to false if it was previously set in state
		data.SslVersionTls12 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.3"); value.Exists() {
		data.SslVersionTls13 = types.BoolValue(true)
	} else if !data.SslVersionTls13.IsNull() {
		// Only set to false if it was previously set in state
		data.SslVersionTls13 = types.BoolValue(false)
	}
}

// End of section. //template:end fromBody

// Section below is generated&owned by "gen/generator.go". //template:begin fromBodyData

func (data *HTTPClientData) fromBody(ctx context.Context, res gjson.Result) {

	prefix := helpers.LastElement(data.getPath()) + "."
	if res.Get(helpers.LastElement(data.getPath())).IsArray() {
		prefix += "0."
	}
	// Check if data is at root level (gNMI response case)
	if !res.Get(helpers.LastElement(data.getPath())).Exists() {
		prefix = ""
	}
	if value := res.Get(prefix + "vrf"); value.Exists() {
		data.Vrf = types.StringValue(value.String())
	}
	if value := res.Get(prefix + "secure-verify-peer.disable"); value.Exists() {
		data.SecureVerifyPeerDisable = types.BoolValue(true)
	} else {
		data.SecureVerifyPeerDisable = types.BoolValue(false)
	}
	if value := res.Get(prefix + "secure-verify-host.disable"); value.Exists() {
		data.SecureVerifyHostDisable = types.BoolValue(true)
	} else {
		data.SecureVerifyHostDisable = types.BoolValue(false)
	}
	if value := res.Get(prefix + "response.timeout"); value.Exists() {
		data.ResponseTimeout = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "connection.timeout"); value.Exists() {
		data.ConnectionTimeout = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "connection.retry"); value.Exists() {
		data.ConnectionRetry = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "source-interface.ipv4.interface"); value.Exists() {
		data.SourceInterfaceIpv4 = types.StringValue(value.String())
	}
	if value := res.Get(prefix + "source-interface.ipv6.interface"); value.Exists() {
		data.SourceInterfaceIpv6 = types.StringValue(value.String())
	}
	if value := res.Get(prefix + "version.default"); value.Exists() {
		data.VersionDefault = types.BoolValue(true)
	} else {
		data.VersionDefault = types.BoolValue(false)
	}
	if value := res.Get(prefix + "version.http1\\.0"); value.Exists() {
		data.Version10 = types.BoolValue(true)
	} else {
		data.Version10 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "version.http1\\.1"); value.Exists() {
		data.Version11 = types.BoolValue(true)
	} else {
		data.Version11 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "tcp-window-scale"); value.Exists() {
		data.TcpWindowScale = types.Int64Value(value.Int())
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.0"); value.Exists() {
		data.SslVersionTls10 = types.BoolValue(true)
	} else {
		data.SslVersionTls10 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.1"); value.Exists() {
		data.SslVersionTls11 = types.BoolValue(true)
	} else {
		data.SslVersionTls11 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.2"); value.Exists() {
		data.SslVersionTls12 = types.BoolValue(true)
	} else {
		data.SslVersionTls12 = types.BoolValue(false)
	}
	if value := res.Get(prefix + "ssl.version.tls1\\.3"); value.Exists() {
		data.SslVersionTls13 = types.BoolValue(true)
	} else {
		data.SslVersionTls13 = types.BoolValue(false)
	}
}

// End of section. //template:end fromBodyData

// Section below is generated&owned by "gen/generator.go". //template:begin getDeletedItems

func (data *HTTPClient) getDeletedItems(ctx context.Context, state HTTPClient) []string {
	deletedItems := make([]string, 0)
	if !state.SslVersionTls13.IsNull() && data.SslVersionTls13.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/ssl/version/tls1.3", state.getPath()))
	}
	if !state.SslVersionTls12.IsNull() && data.SslVersionTls12.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/ssl/version/tls1.2", state.getPath()))
	}
	if !state.SslVersionTls11.IsNull() && data.SslVersionTls11.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/ssl/version/tls1.1", state.getPath()))
	}
	if !state.SslVersionTls10.IsNull() && data.SslVersionTls10.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/ssl/version/tls1.0", state.getPath()))
	}
	if !state.TcpWindowScale.IsNull() && data.TcpWindowScale.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/tcp-window-scale", state.getPath()))
	}
	if !state.Version11.IsNull() && data.Version11.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/version/http1.1", state.getPath()))
	}
	if !state.Version10.IsNull() && data.Version10.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/version/http1.0", state.getPath()))
	}
	if !state.VersionDefault.IsNull() && data.VersionDefault.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/version/default", state.getPath()))
	}
	if !state.SourceInterfaceIpv6.IsNull() && data.SourceInterfaceIpv6.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/source-interface/ipv6/interface", state.getPath()))
	}
	if !state.SourceInterfaceIpv4.IsNull() && data.SourceInterfaceIpv4.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/source-interface/ipv4/interface", state.getPath()))
	}
	if !state.ConnectionRetry.IsNull() && data.ConnectionRetry.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/connection/retry", state.getPath()))
	}
	if !state.ConnectionTimeout.IsNull() && data.ConnectionTimeout.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/connection/timeout", state.getPath()))
	}
	if !state.ResponseTimeout.IsNull() && data.ResponseTimeout.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/response/timeout", state.getPath()))
	}
	if !state.SecureVerifyHostDisable.IsNull() && data.SecureVerifyHostDisable.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/secure-verify-host/disable", state.getPath()))
	}
	if !state.SecureVerifyPeerDisable.IsNull() && data.SecureVerifyPeerDisable.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/secure-verify-peer/disable", state.getPath()))
	}
	if !state.Vrf.IsNull() && data.Vrf.IsNull() {
		deletedItems = append(deletedItems, fmt.Sprintf("%v/vrf", state.getPath()))
	}
	return deletedItems
}

// End of section. //template:end getDeletedItems

// Section below is generated&owned by "gen/generator.go". //template:begin getEmptyLeafsDelete

func (data *HTTPClient) getEmptyLeafsDelete(ctx context.Context, state *HTTPClient) []string {
	emptyLeafsDelete := make([]string, 0)
	// Only delete if state has true and plan has false
	if !data.SslVersionTls13.IsNull() && !data.SslVersionTls13.ValueBool() {
		if state != nil && !state.SslVersionTls13.IsNull() && state.SslVersionTls13.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/ssl/version/tls1.3", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.SslVersionTls12.IsNull() && !data.SslVersionTls12.ValueBool() {
		if state != nil && !state.SslVersionTls12.IsNull() && state.SslVersionTls12.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/ssl/version/tls1.2", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.SslVersionTls11.IsNull() && !data.SslVersionTls11.ValueBool() {
		if state != nil && !state.SslVersionTls11.IsNull() && state.SslVersionTls11.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/ssl/version/tls1.1", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.SslVersionTls10.IsNull() && !data.SslVersionTls10.ValueBool() {
		if state != nil && !state.SslVersionTls10.IsNull() && state.SslVersionTls10.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/ssl/version/tls1.0", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.Version11.IsNull() && !data.Version11.ValueBool() {
		if state != nil && !state.Version11.IsNull() && state.Version11.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/version/http1.1", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.Version10.IsNull() && !data.Version10.ValueBool() {
		if state != nil && !state.Version10.IsNull() && state.Version10.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/version/http1.0", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.VersionDefault.IsNull() && !data.VersionDefault.ValueBool() {
		if state != nil && !state.VersionDefault.IsNull() && state.VersionDefault.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/version/default", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.SecureVerifyHostDisable.IsNull() && !data.SecureVerifyHostDisable.ValueBool() {
		if state != nil && !state.SecureVerifyHostDisable.IsNull() && state.SecureVerifyHostDisable.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/secure-verify-host/disable", data.getXPath()))
		}
	}
	// Only delete if state has true and plan has false
	if !data.SecureVerifyPeerDisable.IsNull() && !data.SecureVerifyPeerDisable.ValueBool() {
		if state != nil && !state.SecureVerifyPeerDisable.IsNull() && state.SecureVerifyPeerDisable.ValueBool() {
			emptyLeafsDelete = append(emptyLeafsDelete, fmt.Sprintf("%v/secure-verify-peer/disable", data.getXPath()))
		}
	}
	return emptyLeafsDelete
}

// End of section. //template:end getEmptyLeafsDelete

// Section below is generated&owned by "gen/generator.go". //template:begin getDeletePaths

func (data *HTTPClient) getDeletePaths(ctx context.Context) []string {
	var deletePaths []string
	if !data.SslVersionTls13.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/ssl/version/tls1.3", data.getPath()))
	}
	if !data.SslVersionTls12.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/ssl/version/tls1.2", data.getPath()))
	}
	if !data.SslVersionTls11.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/ssl/version/tls1.1", data.getPath()))
	}
	if !data.SslVersionTls10.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/ssl/version/tls1.0", data.getPath()))
	}
	if !data.TcpWindowScale.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/tcp-window-scale", data.getPath()))
	}
	if !data.Version11.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/version/http1.1", data.getPath()))
	}
	if !data.Version10.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/version/http1.0", data.getPath()))
	}
	if !data.VersionDefault.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/version/default", data.getPath()))
	}
	if !data.SourceInterfaceIpv6.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/source-interface/ipv6/interface", data.getPath()))
	}
	if !data.SourceInterfaceIpv4.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/source-interface/ipv4/interface", data.getPath()))
	}
	if !data.ConnectionRetry.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/connection/retry", data.getPath()))
	}
	if !data.ConnectionTimeout.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/connection/timeout", data.getPath()))
	}
	if !data.ResponseTimeout.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/response/timeout", data.getPath()))
	}
	if !data.SecureVerifyHostDisable.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/secure-verify-host/disable", data.getPath()))
	}
	if !data.SecureVerifyPeerDisable.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/secure-verify-peer/disable", data.getPath()))
	}
	if !data.Vrf.IsNull() {
		deletePaths = append(deletePaths, fmt.Sprintf("%v/vrf", data.getPath()))
	}

	return deletePaths
}

// End of section. //template:end getDeletePaths

// Section below is generated&owned by "gen/generator.go". //template:begin toBodyXML

func (data HTTPClient) toBodyXML(ctx context.Context, stateArg ...*HTTPClient) string {
	var state *HTTPClient
	if len(stateArg) > 0 {
		state = stateArg[0]
	}
	body := netconf.Body{}
	if !data.Vrf.IsNull() && !data.Vrf.IsUnknown() {
		body = helpers.SetFromXPath(body, data.getXPath()+"/vrf", data.Vrf.ValueString())
	}
	if !data.SecureVerifyPeerDisable.IsNull() && !data.SecureVerifyPeerDisable.IsUnknown() {
		if data.SecureVerifyPeerDisable.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/secure-verify-peer/disable", "")
		}
	}
	if !data.SecureVerifyHostDisable.IsNull() && !data.SecureVerifyHostDisable.IsUnknown() {
		if data.SecureVerifyHostDisable.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/secure-verify-host/disable", "")
		}
	}
	if !data.ResponseTimeout.IsNull() && !data.ResponseTimeout.IsUnknown() {
		body = helpers.SetFromXPath(body, data.getXPath()+"/response/timeout", strconv.FormatInt(data.ResponseTimeout.ValueInt64(), 10))
	}
	if !data.ConnectionTimeout.IsNull() && !data.ConnectionTimeout.IsUnknown() {
		body = helpers.SetFromXPath(body, data.getXPath()+"/connection/timeout", strconv.FormatInt(data.ConnectionTimeout.ValueInt64(), 10))
	}
	if !data.ConnectionRetry.IsNull() && !data.ConnectionRetry.IsUnknown() {
		body = helpers.SetFromXPath(body, data.getXPath()+"/connection/retry", strconv.FormatInt(data.ConnectionRetry.ValueInt64(), 10))
	}
	if !data.SourceInterfaceIpv4.IsNull() && !data.SourceInterfaceIpv4.IsUnknown() {
		body = helpers.SetFromXPath(body, data.getXPath()+"/source-interface/ipv4/interface", data.SourceInterfaceIpv4.ValueString())
	}
	if !data.SourceInterfaceIpv6.IsNull() && !data.SourceInterfaceIpv6.IsUnknown() {
		body = helpers.SetFromXPath(body, data.getXPath()+"/source-interface/ipv6/interface", data.SourceInterfaceIpv6.ValueString())
	}
	if !data.VersionDefault.IsNull() && !data.VersionDefault.IsUnknown() {
		if data.VersionDefault.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/version/default", "")
		}
	}
	if !data.Version10.IsNull() && !data.Version10.IsUnknown() {
		if data.Version10.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/version/http1.0", "")
		}
	}
	if !data.Version11.IsNull() && !data.Version11.IsUnknown() {
		if data.Version11.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/version/http1.1", "")
		}
	}
	if !data.TcpWindowScale.IsNull() && !data.TcpWindowScale.IsUnknown() {
		body = helpers.SetFromXPath(body, data.getXPath()+"/tcp-window-scale", strconv.FormatInt(data.TcpWindowScale.ValueInt64(), 10))
	}
	if !data.SslVersionTls10.IsNull() && !data.SslVersionTls10.IsUnknown() {
		if data.SslVersionTls10.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/ssl/version/tls1.0", "")
		}
	}
	if !data.SslVersionTls11.IsNull() && !data.SslVersionTls11.IsUnknown() {
		if data.SslVersionTls11.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/ssl/version/tls1.1", "")
		}
	}
	if !data.SslVersionTls12.IsNull() && !data.SslVersionTls12.IsUnknown() {
		if data.SslVersionTls12.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/ssl/version/tls1.2", "")
		}
	}
	if !data.SslVersionTls13.IsNull() && !data.SslVersionTls13.IsUnknown() {
		if data.SslVersionTls13.ValueBool() {
			body = helpers.SetFromXPath(body, data.getXPath()+"/ssl/version/tls1.3", "")
		}
	}
	bodyString, err := helpers.BodyToNestedXML(body)
	if err != nil {
		tflog.Error(ctx, fmt.Sprintf("Error converting body to nested XML: %s", err))
		// If there's an error (e.g., invalid path syntax for xmlns attributes), return empty string
		// This allows XML namespace siblings to be handled separately
		return ""
	}
	bodyString = helpers.AddNamespaceToRootElement(bodyString, data.getXPath())
	// On Create, seed the keyed base node when no leaves were emitted so a
	// keys-only entry (e.g. address-family ipv4 unicast) isn't sent as an empty
	// body, which EditConfig skips — creating drift. Uses default merge
	// (RFC 6241 §7.2); getXPath()'s key gives a valid minimal list entry
	// (RFC 7950 §7.8.2). Create-only (state == nil) leaves Update untouched.
	if bodyString == "" && state == nil {
		seededBody, seedErr := helpers.BodyToNestedXML(helpers.SetFromXPath(netconf.Body{}, data.getXPath(), ""))
		if seedErr != nil {
			tflog.Error(ctx, fmt.Sprintf("Error seeding keys-only base node: %s", seedErr))
		} else {
			bodyString = helpers.AddNamespaceToRootElement(seededBody, data.getXPath())
		}
	}
	// Append delete XML for empty bool leafs (false values that need explicit removal)
	for _, deletePath := range data.getEmptyLeafsDelete(ctx, state) {
		bodyString += helpers.RemoveFromXPath(netconf.Body{}, deletePath).Res()
	}
	tflog.Debug(ctx, fmt.Sprintf("toBodyXML: generated body length: %d", len(bodyString)))
	return bodyString
}

// End of section. //template:end toBodyXML

// Section below is generated&owned by "gen/generator.go". //template:begin updateFromBodyXML

func (data *HTTPClient) updateFromBodyXML(ctx context.Context, res xmldot.Result) {
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/vrf"); value.Exists() && !data.Vrf.IsNull() {
		data.Vrf = types.StringValue(value.String())
	} else if data.Vrf.IsNull() {
		data.Vrf = types.StringNull()
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/secure-verify-peer/disable"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SecureVerifyPeerDisable.IsNull() {
			data.SecureVerifyPeerDisable = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SecureVerifyPeerDisable.IsNull() {
			data.SecureVerifyPeerDisable = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/secure-verify-host/disable"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SecureVerifyHostDisable.IsNull() {
			data.SecureVerifyHostDisable = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SecureVerifyHostDisable.IsNull() {
			data.SecureVerifyHostDisable = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/response/timeout"); value.Exists() && !data.ResponseTimeout.IsNull() {
		data.ResponseTimeout = types.Int64Value(value.Int())
	} else if data.ResponseTimeout.IsNull() {
		data.ResponseTimeout = types.Int64Null()
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/connection/timeout"); value.Exists() && !data.ConnectionTimeout.IsNull() {
		data.ConnectionTimeout = types.Int64Value(value.Int())
	} else if data.ConnectionTimeout.IsNull() {
		data.ConnectionTimeout = types.Int64Null()
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/connection/retry"); value.Exists() && !data.ConnectionRetry.IsNull() {
		data.ConnectionRetry = types.Int64Value(value.Int())
	} else if data.ConnectionRetry.IsNull() {
		data.ConnectionRetry = types.Int64Null()
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/source-interface/ipv4/interface"); value.Exists() && !data.SourceInterfaceIpv4.IsNull() {
		data.SourceInterfaceIpv4 = types.StringValue(value.String())
	} else if data.SourceInterfaceIpv4.IsNull() {
		data.SourceInterfaceIpv4 = types.StringNull()
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/source-interface/ipv6/interface"); value.Exists() && !data.SourceInterfaceIpv6.IsNull() {
		data.SourceInterfaceIpv6 = types.StringValue(value.String())
	} else if data.SourceInterfaceIpv6.IsNull() {
		data.SourceInterfaceIpv6 = types.StringNull()
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/default"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.VersionDefault.IsNull() {
			data.VersionDefault = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.VersionDefault.IsNull() {
			data.VersionDefault = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/http1.0"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.Version10.IsNull() {
			data.Version10 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.Version10.IsNull() {
			data.Version10 = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/http1.1"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.Version11.IsNull() {
			data.Version11 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.Version11.IsNull() {
			data.Version11 = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/tcp-window-scale"); value.Exists() && !data.TcpWindowScale.IsNull() {
		data.TcpWindowScale = types.Int64Value(value.Int())
	} else if data.TcpWindowScale.IsNull() {
		data.TcpWindowScale = types.Int64Null()
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.0"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls10.IsNull() {
			data.SslVersionTls10 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls10.IsNull() {
			data.SslVersionTls10 = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.1"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls11.IsNull() {
			data.SslVersionTls11 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls11.IsNull() {
			data.SslVersionTls11 = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.2"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls12.IsNull() {
			data.SslVersionTls12 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls12.IsNull() {
			data.SslVersionTls12 = types.BoolNull()
		}
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.3"); value.Exists() {
		// Only set to true if it was already in the plan (not null)
		if !data.SslVersionTls13.IsNull() {
			data.SslVersionTls13 = types.BoolValue(true)
		}
	} else {
		// For presence-based booleans, only set to null if it's already null
		if data.SslVersionTls13.IsNull() {
			data.SslVersionTls13 = types.BoolNull()
		}
	}
}

// End of section. //template:end updateFromBodyXML

// Section below is generated&owned by "gen/generator.go". //template:begin fromBodyXML

func (data *HTTPClient) fromBodyXML(ctx context.Context, res xmldot.Result) {
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/vrf"); value.Exists() {
		data.Vrf = types.StringValue(value.String())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/secure-verify-peer/disable"); value.Exists() {
		data.SecureVerifyPeerDisable = types.BoolValue(true)
	} else {
		data.SecureVerifyPeerDisable = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/secure-verify-host/disable"); value.Exists() {
		data.SecureVerifyHostDisable = types.BoolValue(true)
	} else {
		data.SecureVerifyHostDisable = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/response/timeout"); value.Exists() {
		data.ResponseTimeout = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/connection/timeout"); value.Exists() {
		data.ConnectionTimeout = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/connection/retry"); value.Exists() {
		data.ConnectionRetry = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/source-interface/ipv4/interface"); value.Exists() {
		data.SourceInterfaceIpv4 = types.StringValue(value.String())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/source-interface/ipv6/interface"); value.Exists() {
		data.SourceInterfaceIpv6 = types.StringValue(value.String())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/default"); value.Exists() {
		data.VersionDefault = types.BoolValue(true)
	} else {
		data.VersionDefault = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/http1.0"); value.Exists() {
		data.Version10 = types.BoolValue(true)
	} else {
		data.Version10 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/http1.1"); value.Exists() {
		data.Version11 = types.BoolValue(true)
	} else {
		data.Version11 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/tcp-window-scale"); value.Exists() {
		data.TcpWindowScale = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.0"); value.Exists() {
		data.SslVersionTls10 = types.BoolValue(true)
	} else {
		data.SslVersionTls10 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.1"); value.Exists() {
		data.SslVersionTls11 = types.BoolValue(true)
	} else {
		data.SslVersionTls11 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.2"); value.Exists() {
		data.SslVersionTls12 = types.BoolValue(true)
	} else {
		data.SslVersionTls12 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.3"); value.Exists() {
		data.SslVersionTls13 = types.BoolValue(true)
	} else {
		data.SslVersionTls13 = types.BoolValue(false)
	}
}

// End of section. //template:end fromBodyXML

// Section below is generated&owned by "gen/generator.go". //template:begin fromBodyDataXML

func (data *HTTPClientData) fromBodyXML(ctx context.Context, res xmldot.Result) {
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/vrf"); value.Exists() {
		data.Vrf = types.StringValue(value.String())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/secure-verify-peer/disable"); value.Exists() {
		data.SecureVerifyPeerDisable = types.BoolValue(true)
	} else {
		data.SecureVerifyPeerDisable = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/secure-verify-host/disable"); value.Exists() {
		data.SecureVerifyHostDisable = types.BoolValue(true)
	} else {
		data.SecureVerifyHostDisable = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/response/timeout"); value.Exists() {
		data.ResponseTimeout = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/connection/timeout"); value.Exists() {
		data.ConnectionTimeout = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/connection/retry"); value.Exists() {
		data.ConnectionRetry = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/source-interface/ipv4/interface"); value.Exists() {
		data.SourceInterfaceIpv4 = types.StringValue(value.String())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/source-interface/ipv6/interface"); value.Exists() {
		data.SourceInterfaceIpv6 = types.StringValue(value.String())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/default"); value.Exists() {
		data.VersionDefault = types.BoolValue(true)
	} else {
		data.VersionDefault = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/http1.0"); value.Exists() {
		data.Version10 = types.BoolValue(true)
	} else {
		data.Version10 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/version/http1.1"); value.Exists() {
		data.Version11 = types.BoolValue(true)
	} else {
		data.Version11 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/tcp-window-scale"); value.Exists() {
		data.TcpWindowScale = types.Int64Value(value.Int())
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.0"); value.Exists() {
		data.SslVersionTls10 = types.BoolValue(true)
	} else {
		data.SslVersionTls10 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.1"); value.Exists() {
		data.SslVersionTls11 = types.BoolValue(true)
	} else {
		data.SslVersionTls11 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.2"); value.Exists() {
		data.SslVersionTls12 = types.BoolValue(true)
	} else {
		data.SslVersionTls12 = types.BoolValue(false)
	}
	if value := helpers.GetFromXPath(res, "data/"+data.getXPath()+"/ssl/version/tls1.3"); value.Exists() {
		data.SslVersionTls13 = types.BoolValue(true)
	} else {
		data.SslVersionTls13 = types.BoolValue(false)
	}
}

// End of section. //template:end fromBodyDataXML

// Section below is generated&owned by "gen/generator.go". //template:begin addDeletedItemsXML

func (data *HTTPClient) addDeletedItemsXML(ctx context.Context, state HTTPClient, body string) string {
	// Start with an empty body - we'll build up the delete operations
	b := netconf.Body{}
	deletedPaths := make(map[string]bool)
	_ = deletedPaths // Avoid unused variable error when no delete_parent attributes exist
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.SslVersionTls13.IsNull() && state.SslVersionTls13.ValueBool() && data.SslVersionTls13.IsNull() {
		deletePath := state.getXPath() + "/ssl/version/tls1.3"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.SslVersionTls12.IsNull() && state.SslVersionTls12.ValueBool() && data.SslVersionTls12.IsNull() {
		deletePath := state.getXPath() + "/ssl/version/tls1.2"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.SslVersionTls11.IsNull() && state.SslVersionTls11.ValueBool() && data.SslVersionTls11.IsNull() {
		deletePath := state.getXPath() + "/ssl/version/tls1.1"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.SslVersionTls10.IsNull() && state.SslVersionTls10.ValueBool() && data.SslVersionTls10.IsNull() {
		deletePath := state.getXPath() + "/ssl/version/tls1.0"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	if !state.TcpWindowScale.IsNull() && data.TcpWindowScale.IsNull() {
		deletePath := state.getXPath() + "/tcp-window-scale"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.Version11.IsNull() && state.Version11.ValueBool() && data.Version11.IsNull() {
		deletePath := state.getXPath() + "/version/http1.1"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.Version10.IsNull() && state.Version10.ValueBool() && data.Version10.IsNull() {
		deletePath := state.getXPath() + "/version/http1.0"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.VersionDefault.IsNull() && state.VersionDefault.ValueBool() && data.VersionDefault.IsNull() {
		deletePath := state.getXPath() + "/version/default"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	if !state.SourceInterfaceIpv6.IsNull() && data.SourceInterfaceIpv6.IsNull() {
		deletePath := state.getXPath() + "/source-interface/ipv6/interface"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	if !state.SourceInterfaceIpv4.IsNull() && data.SourceInterfaceIpv4.IsNull() {
		deletePath := state.getXPath() + "/source-interface/ipv4/interface"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	if !state.ConnectionRetry.IsNull() && data.ConnectionRetry.IsNull() {
		deletePath := state.getXPath() + "/connection/retry"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	if !state.ConnectionTimeout.IsNull() && data.ConnectionTimeout.IsNull() {
		deletePath := state.getXPath() + "/connection/timeout"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	if !state.ResponseTimeout.IsNull() && data.ResponseTimeout.IsNull() {
		deletePath := state.getXPath() + "/response/timeout"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.SecureVerifyHostDisable.IsNull() && state.SecureVerifyHostDisable.ValueBool() && data.SecureVerifyHostDisable.IsNull() {
		deletePath := state.getXPath() + "/secure-verify-host/disable"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	// For boolean fields, only delete if state was true (presence container was set)
	if !state.SecureVerifyPeerDisable.IsNull() && state.SecureVerifyPeerDisable.ValueBool() && data.SecureVerifyPeerDisable.IsNull() {
		deletePath := state.getXPath() + "/secure-verify-peer/disable"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}
	if !state.Vrf.IsNull() && data.Vrf.IsNull() {
		deletePath := state.getXPath() + "/vrf"
		// Check if a parent path is already marked for deletion
		parentAlreadyDeleted := false
		for dp := range deletedPaths {
			if strings.HasPrefix(deletePath, dp+"/") {
				parentAlreadyDeleted = true
				break
			}
		}
		if !parentAlreadyDeleted && !deletedPaths[deletePath] {
			b = helpers.RemoveFromXPath(b, deletePath)
			deletedPaths[deletePath] = true
		}
	}

	//b = helpers.CleanupRedundantRemoveOperations(b)
	return b.Res()
}

// End of section. //template:end addDeletedItemsXML

// Section below is generated&owned by "gen/generator.go". //template:begin addDeletePathsXML

func (data *HTTPClient) addDeletePathsXML(ctx context.Context, body string) string {
	b := netconf.NewBody(body)
	if !data.SslVersionTls13.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/ssl/version/tls1.3")
	}
	if !data.SslVersionTls12.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/ssl/version/tls1.2")
	}
	if !data.SslVersionTls11.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/ssl/version/tls1.1")
	}
	if !data.SslVersionTls10.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/ssl/version/tls1.0")
	}
	if !data.TcpWindowScale.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/tcp-window-scale")
	}
	if !data.Version11.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/version/http1.1")
	}
	if !data.Version10.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/version/http1.0")
	}
	if !data.VersionDefault.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/version/default")
	}
	if !data.SourceInterfaceIpv6.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/source-interface/ipv6/interface")
	}
	if !data.SourceInterfaceIpv4.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/source-interface/ipv4/interface")
	}
	if !data.ConnectionRetry.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/connection/retry")
	}
	if !data.ConnectionTimeout.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/connection/timeout")
	}
	if !data.ResponseTimeout.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/response/timeout")
	}
	if !data.SecureVerifyHostDisable.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/secure-verify-host/disable")
	}
	if !data.SecureVerifyPeerDisable.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/secure-verify-peer/disable")
	}
	if !data.Vrf.IsNull() {
		b = helpers.RemoveFromXPath(b, data.getXPath()+"/vrf")
	}

	return b.Res()
}

// End of section. //template:end addDeletePathsXML
